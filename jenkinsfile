



node {
    stage('Shared') {
        steps {
                script {
                    bat(script: '@git submodule update --init --recursive')
                }
            }
    }

        load 'TestSubmodule/jenkinsfile'
 
}
