pipeline {
    agent any
    stages {
		stage('show jenkisfile version') {
            steps {
                script {
					echo "-----------------------------------------------"
                    echo "---verze jenkinsfile: ${JENKINSFILE_VERSION}---"
					echo "------------------------------------------------"
                }
            }
        }
	
       
    }
}